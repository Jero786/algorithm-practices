/**
 * With a good pivot, will sort with a time complexity: O(nlogn(2)) - Linearithmic
 * This is due, if we choose a right pivot, near to the center of the partition of the array.
 *
 * With a bad pivot, like take as a pivot the last item of the array, will be end up in a O(n^2) -
 * https://www.youtube.com/watch?v=PgBzjlCcFvc
 * @param array
 * @returns {*[]}
 */
function sortArray(array = []) {
  // TO BE CONTINUE...
  return array;
}

module.exports = {
  sortArray
}